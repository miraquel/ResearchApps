@model ResearchApps.Service.Vm.GoodsReceiptVm

@{
    ViewBag.Title = "Create Goods Receipt";
    ViewBag.pTitle = "Create Goods Receipt";
    Layout = "_Layout";
}

<div x-data="grCreate()" x-init="init()">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0">Create New Goods Receipt</h5>
                </div>
                <div class="card-body">
                    <form id="gr-form" 
                          method="post"
                          action="/GoodsReceipts/Create">
                        @Html.AntiForgeryToken()
                        
                        <!-- Validation Summary -->
                        <div id="validation-summary" x-show="Object.keys(errors).length > 0" x-cloak>
                            <div class="alert alert-danger">
                                <template x-for="(error, field) in errors" :key="field">
                                    <div x-text="error"></div>
                                </template>
                            </div>
                        </div>
                        
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label for="Header_GrDate" class="form-label">GR Date <span class="text-danger">*</span></label>
                                    <div class="input-group">
                                        <input type="text" 
                                               id="Header_GrDate" 
                                               name="Header.GrDate"
                                               class="form-control"
                                               x-ref="grDatePicker"
                                               data-flatpickr
                                               :class="{ 'is-invalid': errors.GrDate }"
                                               required />
                                        <span class="input-group-text"><i class="ri-calendar-line"></i></span>
                                    </div>
                                    <div class="invalid-feedback" x-show="errors.GrDate" x-text="errors.GrDate"></div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label for="Header_SupplierId" class="form-label">Supplier <span class="text-danger">*</span></label>
                                    <select class="form-control form-select"
                                            id="Header_SupplierId" 
                                            name="Header.SupplierId"
                                            x-ref="supplierSelect"
                                            data-tomselect
                                            data-url="/api/Suppliers/cbo"
                                            :class="{ 'is-invalid': errors.SupplierId }"
                                            required>
                                        <option value="">Select Supplier</option>
                                    </select>
                                    <div class="invalid-feedback" x-show="errors.SupplierId" x-text="errors.SupplierId"></div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label for="Header_RefNo" class="form-label">Reference No (Optional)</label>
                                    <input type="text" 
                                           id="Header_RefNo" 
                                           name="Header.RefNo"
                                           class="form-control"
                                           placeholder="Enter reference number"
                                           maxlength="50" />
                                </div>
                            </div>
                        </div>
                        
                        <div class="row mb-3">
                            <div class="col-md-12">
                                <div class="form-group">
                                    <label for="Header_Notes" class="form-label">Notes (Optional)</label>
                                    <textarea id="Header_Notes"
                                              name="Header.Notes"
                                              class="form-control" 
                                              rows="3" 
                                              placeholder="Enter notes (optional)"
                                              x-model="header.notes"></textarea>
                                </div>
                            </div>
                        </div>
                        
                        <div class="form-group d-flex gap-2">
                            <button type="submit" 
                                    class="btn btn-primary"
                                    :disabled="isSubmitting"
                                    @@click.prevent="confirmSubmit()">
                                <span x-show="!isSubmitting">
                                    <i class="ri-save-line me-1"></i> Save
                                </span>
                                <span x-show="isSubmitting" x-cloak>
                                    <span class="spinner-border spinner-border-sm me-1" role="status"></span>
                                    Creating...
                                </span>
                            </button>
                            <a href="/GoodsReceipts" class="btn btn-secondary">
                                <i class="ri-arrow-left-line me-1"></i> Cancel
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Confirmation Modal -->
    <div class="modal fade" 
         :class="{ 'show d-block': showConfirmModal }"
         x-show="showConfirmModal"
         x-cloak
         tabindex="-1"
         @@click.self="showConfirmModal = false"
         @@keydown.escape.window="showConfirmModal = false">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Confirm Create</h5>
                    <button type="button" class="btn-close" @@click="showConfirmModal = false"></button>
                </div>
                <div class="modal-body">
                    <p>Are you sure you want to create this goods receipt?</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" @@click="showConfirmModal = false">Cancel</button>
                    <button type="button" class="btn btn-primary" @@click="submitForm()">
                        <i class="ri-check-line me-1"></i> Confirm
                    </button>
                </div>
            </div>
        </div>
    </div>
    <div class="modal-backdrop fade show" x-show="showConfirmModal" x-cloak></div>
</div>

@section scripts {
    <script src="~/js/goods-receipts/gr-create.js"></script>
}
