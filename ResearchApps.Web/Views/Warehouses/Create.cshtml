@model ResearchApps.Service.Vm.WarehouseVm

@{
    ViewBag.Title = "Create Warehouse";
    ViewBag.pTitle = "Create Warehouse";
    Layout = "_Layout";
}

@* Form card *@
<div class="card">
    <div class="card-body">
        <form id="warehouse-form" asp-action="Create" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="StatusId" value="1" />
            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

            <div class="row">
                <div class="col-md-6">
                    <div class="mb-3">
                        <label asp-for="WhName" class="form-label"></label>
                        <input asp-for="WhName" class="form-control" placeholder="Enter warehouse name" required />
                        <span asp-validation-for="WhName" class="text-danger"></span>
                    </div>
                </div>
            </div>

            <hr class="my-4" />

            <div class="d-flex gap-2">
                <button type="button" class="btn btn-primary"
                        data-confirm
                        data-confirm-modal="createWarehouseModal"
                        data-confirm-form="#warehouse-form"
                        data-confirm-validate="true"
                        data-confirm-title="Confirm Create"
                        data-confirm-message="Are you sure you want to create this warehouse?">
                    <i class="ri-save-line align-bottom me-1"></i> Create
                </button>
                <a asp-action="Index" class="btn btn-soft-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>
