@using ResearchApps.Common.Constants
@model ResearchApps.Service.Vm.WarehouseVm

@{
    ViewBag.Title = "Warehouse Details";
    ViewBag.pTitle = $"Warehouse {Model.WhId}";
    Layout = "_Layout";
}

<div class="card">
    <div class="card-header">
        <div class="row">
            <div class="col d-flex justify-content-between align-items-center">
                <ul class="nav nav-tabs-custom rounded card-header-tabs border-bottom-0" role="tablist">
                    <li class="nav-item" role="presentation">
                        <a class="nav-link active" data-bs-toggle="tab" href="#overview">Overview</a>
                    </li>
                    <li class="nav-item" role="presentation">
                        <a class="nav-link" data-bs-toggle="tab" href="#log">Log</a>
                    </li>
                </ul>
                <div class="position-absolute top-0 end-0">
                    @if(User.HasClaim("permission", PermissionConstants.Warehouses.Edit))
                    {
                        <a href="@Url.Action("Edit", "Warehouses", new { id = Model.WhId })" class="btn btn-sm btn-success me-2">
                            <i class="ri-edit-line"></i> Edit
                        </a>
                    }
                    @if (User.HasClaim("permission", PermissionConstants.Warehouses.Delete))
                    {
                        <a href="@Url.Action("Delete", "Warehouses", new { id = Model.WhId })" class="btn btn-sm btn-danger">
                            <i class="ri-delete-bin-line"></i> Delete
                        </a>
                    }
                </div>
            </div>
        </div>
    </div>
    <div class="card-body">
        <div class="tab-content">
            <div class="tab-pane active" id="overview" role="tabpanel">
                <div class="row">
                    <div class="col-md-6">
                        <table class="table table-borderless">
                            <tr>
                                <td class="fw-bold" style="width: 150px;">@Html.DisplayNameFor(m => m.WhId)</td>
                                <td>@Model.WhId</td>
                            </tr>
                            <tr>
                                <td class="fw-bold">@Html.DisplayNameFor(m => m.WhName)</td>
                                <td>@Model.WhName</td>
                            </tr>
                            <tr>
                                <td class="fw-bold">@Html.DisplayNameFor(m => m.StatusName)</td>
                                <td>@Html.Raw(Model.StatusName)</td>
                            </tr>
                        </table>
                    </div>
                </div>
            </div>
            <div class="tab-pane" id="log" role="tabpanel">
                <div class="row">
                    <div class="col-md-6">
                        <table class="table table-borderless">
                            <tr>
                                <td class="fw-bold" style="width: 150px;">@Html.DisplayNameFor(m => m.CreatedDate)</td>
                                <td>@Model.CreatedDate</td>
                            </tr>
                            <tr>
                                <td class="fw-bold">@Html.DisplayNameFor(m => m.CreatedBy)</td>
                                <td>@Model.CreatedBy</td>
                            </tr>
                        </table>
                    </div>
                    <div class="col-md-6">
                        <table class="table table-borderless">
                            <tr>
                                <td class="fw-bold" style="width: 150px;">@Html.DisplayNameFor(m => m.ModifiedDate)</td>
                                <td>@Model.ModifiedDate</td>
                            </tr>
                            <tr>
                                <td class="fw-bold">@Html.DisplayNameFor(m => m.ModifiedBy)</td>
                                <td>@Model.ModifiedBy</td>
                            </tr>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
