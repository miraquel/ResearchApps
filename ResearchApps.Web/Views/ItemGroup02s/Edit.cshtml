@model ResearchApps.Service.Vm.ItemGroup02Vm

@{
    ViewBag.Title = "Edit Item Group 02";
    ViewBag.pTitle = $"Edit Item Group 02: {Model.ItemGroup02Name}";
    Layout = "_Layout";
}

<div x-data="itemgroup02Edit()">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0">Edit Item Group 02</h2>
        <a asp-action="Index" class="btn btn-soft-secondary">
            <i class="ri-arrow-left-line align-bottom me-1"></i> Back to List
        </a>
    </div>

    <div class="card">
        <div class="card-body">
            <form id="itemgroup02-form" asp-action="Edit" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="ItemGroup02Id" />
                <input type="hidden" asp-for="StatusId" />
                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label asp-for="ItemGroup02Name" class="form-label"></label>
                            <input asp-for="ItemGroup02Name" class="form-control" placeholder="Enter name" required />
                            <span asp-validation-for="ItemGroup02Name" class="text-danger"></span>
                        </div>
                    </div>
                </div>

                <hr class="my-4" />

                <div class="d-flex gap-2">
                    <button type="button" class="btn btn-primary"
                            data-confirm
                            data-confirm-modal="saveItemGroup02Modal"
                            data-confirm-form="#itemgroup02-form"
                            data-confirm-validate="true"
                            data-confirm-title="Confirm Save"
                            data-confirm-message="Are you sure you want to save changes to this item group?">
                        <i class="ri-save-line align-bottom me-1"></i> Save
                    </button>
                    <a asp-action="Index" class="btn btn-soft-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section scripts {
    <script src="~/js/itemgroup02s/itemgroup02-edit.js"></script>
}
