@model ResearchApps.Service.Vm.ItemDeptVm

@{
    ViewBag.Title = "Create Item Department";
    ViewBag.pTitle = "Create Item Department";
    Layout = "_Layout";
}

@* Alpine.js component for item department creation form *@
<div x-data="itemdeptCreate()">
    @* Form card *@
    <div class="card">
        <div class="card-body">
            <form id="itemdept-form" asp-action="Create" method="post">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label asp-for="ItemDeptName" class="form-label"></label>
                            <input asp-for="ItemDeptName" class="form-control" placeholder="Enter department name" required />
                            <span asp-validation-for="ItemDeptName" class="text-danger"></span>
                        </div>

                        <input type="hidden" asp-for="StatusId" value="1" />
                    </div>
                </div>

                <hr class="my-4" />

                <div class="d-flex gap-2">
                    <button type="button" class="btn btn-primary"
                            data-confirm
                            data-confirm-modal="createItemDeptModal"
                            data-confirm-form="#itemdept-form"
                            data-confirm-validate="true"
                            data-confirm-title="Confirm Create"
                            data-confirm-message="Are you sure you want to create this item department?">
                        <i class="ri-save-line align-bottom me-1"></i> Create
                    </button>
                    <a asp-action="Index" class="btn btn-soft-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section scripts {
    <script src="~/js/itemdepts/itemdept-create.js"></script>
}

