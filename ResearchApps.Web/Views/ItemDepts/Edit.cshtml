@model ResearchApps.Service.Vm.ItemDeptVm

@{
    ViewBag.Title = "Edit Item Department";
    ViewBag.pTitle = $"Edit Item Department: {Model.ItemDeptName}";
    Layout = "_Layout";
}

@* Alpine.js component for item department edit form *@
<div x-data="itemdeptEdit(@Model.StatusId)">
    @* Form card *@
    <div class="card">
        <div class="card-body">
            <form id="itemdept-form" asp-action="Edit" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="ItemDeptId" />
                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label asp-for="ItemDeptName" class="form-label"></label>
                            <input asp-for="ItemDeptName" class="form-control" placeholder="Enter department name" required />
                            <span asp-validation-for="ItemDeptName" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="StatusId" class="form-label"></label>
                            <select asp-for="StatusId" class="form-select"></select>
                            <span asp-validation-for="StatusId" class="text-danger"></span>
                        </div>
                    </div>
                </div>

                <hr class="my-4" />

                <div class="d-flex gap-2">
                    <button type="button" class="btn btn-primary"
                            data-confirm
                            data-confirm-modal="saveItemDeptModal"
                            data-confirm-form="#itemdept-form"
                            data-confirm-validate="true"
                            data-confirm-title="Confirm Save"
                            data-confirm-message="Are you sure you want to save changes to this item department?">
                        <i class="ri-save-line align-bottom me-1"></i> Save
                    </button>
                    <a asp-action="Index" class="btn btn-soft-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section scripts {
    <script src="~/js/itemdepts/itemdept-edit.js"></script>
}

