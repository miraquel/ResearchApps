@model ResearchApps.Service.Vm.Common.PagedListVm<ResearchApps.Service.Vm.SalesInvoiceHeaderVm>

@if (Model.Items.Any())
{
    foreach (var item in Model.Items)
    {
        <tr>
            <td class="text-center fw-semibold">@item.SiId</td>
            <td class="text-center">@item.SiDate.ToString("dd MMM yyyy")</td>
            <td>@item.CustomerName</td>
            <td class="text-center">@(item.PoNo ?? "-")</td>
            <td class="text-center">@(item.TaxNo ?? "-")</td>
            <td class="text-end fw-semibold">@item.Amount.ToString("N0")</td>
            <td class="text-center">
                @Html.Raw(item.SiStatusName)
            </td>
            <td class="text-center">
                <div class="dropdown d-inline-block">
                    <button class="btn btn-soft-secondary btn-sm dropdown" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="ri-more-fill align-middle"></i>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li>
                            <a href="/SalesInvoices/Details/@item.RecId" class="dropdown-item">
                                <i class="ri-eye-fill align-bottom me-2 text-muted"></i> View
                            </a>
                        </li>
                        @if (item.SiStatusId == 0 && item.CreatedBy == User.Identity?.Name)
                        {
                            <li>
                                <a href="/SalesInvoices/Edit/@item.RecId" class="dropdown-item">
                                    <i class="ri-pencil-fill align-bottom me-2 text-muted"></i> Edit
                                </a>
                            </li>
                        }
                    </ul>
                </div>
            </td>
        </tr>
    }
}
else
{
    <tr>
        <td colspan="8" class="text-center text-muted py-4">
            <i class="ri-inbox-line fs-2"></i>
            <p class="mb-0 mt-2">No sales invoices found</p>
        </td>
    </tr>
}
